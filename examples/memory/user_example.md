---
name: User Profile
description: Who the user is and how they'd like to collaborate
type: user
canonicality: reference
---

# Who I am

Replace this with a few lines describing your role, technical background, and the projects you currently care about. Claude reads this at session start and uses it to calibrate explanations and suggestions.

Example:
> Senior backend engineer, 8 years Go + Python, now learning React for a side project. Prefer explanations that frame frontend concepts in terms of server-side analogues. Currently splitting time between two repos: a payments service (work) and a weekend Zettelkasten experiment (this one).

## Collaboration preferences

- **Communication style** — e.g. "terse, no trailing summaries"
- **Review bar** — e.g. "run tests before declaring done; CI green counts, green-on-my-laptop doesn't"
- **Domains off-limits** — e.g. "don't touch anything in `legacy/` without asking"
